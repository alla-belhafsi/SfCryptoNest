{% extends 'base.html.twig' %}

{% block title %}CryptoNest{% endblock %}

{% block body %}
    {# {% if app.user %}
    <div class="profil-card">
        <div class="user-logo">
            <img src="{{ asset('img/userOnline.jpeg') }}" alt="User Logo" width="50" height="44">
            <p class="roles">{{ app.user.roles|join('<br>')|raw }}</p>
        </div>
        <div>
            <p>{{ app.user.firstname }} {{ app.user.lastname }}</p>
            <p>{{ app.user.birthdate|date("d.m.Y") }}</p> 
            <p>{{ app.user.email }}</p>
            <p>email:
                {% if app.user.isVerified == 1 %}
                    Verified
                {% else %}
                    Non-Verified
                {% endif %}
            </p>
        </div>
    </div>   
    {% endif %} #}
    <div id="home-container">
        {% for property in properties %}
            <div class="property-card">
                <div class="property-card-header">
                    <img src="{{ property.cover }}" alt="cover">
                    <div class="property-heart">
                        <a href=""><i class="fa-regular fa-heart"></i></a>
                    </div>
                </div>
                <div class="property-card-text">
                    <h3>{{ property.title }}</h3>
                    <p>{{ property.content|slice(0, 187) }}...</p>
                    <strong>{{ property.price }} € per day</strong>
                </div>
            </div>
        {% endfor %}
    </div>
    

{# <style>
    .example-wrapper { margin: 1em auto; max-width: 800px; width: 95%; font: 18px/1.5 sans-serif; }
    .example-wrapper code { background: #F5F5F5; padding: 2px 6px; }
</style>

<div class="example-wrapper">
    <h1>Hello {{ controller_name }}! ✅</h1>

    This friendly message is coming from:
    <ul>
        <li>Your controller at <code>D:/laragon/www/alla_belhafsi/FirstSymfony/SfCryptoNest/SfCryptoNest/src/Controller/HomeController.php</code></li>
        <li>Your template at <code>D:/laragon/www/alla_belhafsi/FirstSymfony/SfCryptoNest/SfCryptoNest/templates/home/index.html.twig</code></li>
    </ul>
</div> #}
{% endblock %}
